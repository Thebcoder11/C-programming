#include<stdio.h>
int main(){
    int a;
    if(3+2%5){
        printf("This works\n");
    }
    if(a=10){
        printf("Even this works\n");
    }
    if('@'){
        printf("Surprisingly Even this works");
    }
}
// inside if there is expression if it is not 0 it will always work
// all special character all number will work