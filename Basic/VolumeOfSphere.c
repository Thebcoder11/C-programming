#include<stdio.h>
int main(){
    float radius = 5;
    float pi = 3.1415;
    float volume = 4 * pi * radius * radius * radius / 3;
    printf("The Volume of the sphere is : %f",volume);
    return 0;
}