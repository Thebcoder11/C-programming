#include<stdio.h>
int main(){
    int x = 4;
    printf("%d",x);
    x = 8;
    printf("\n%d",x);
    return 0;
}

// %d is a format specifier
// x is a box in which value is stored it does not mean 
// x is equal to 4
// it is different from maths

