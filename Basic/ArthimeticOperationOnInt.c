#include<stdio.h>
int main(){
    int x = 5;
    int y = 2;
    printf("%d",x+y);
    printf("\n%d",x-y);
    printf("\n%d",x*y);
    printf("\n%d",x/y);
    int z = 3.14;
    printf("\n%d",z);
    return 0;
}
// The Divison operator does not devide further if remainder
// is not divisble by divident
// it only gives the quotient value in integer not in float
// integer cannot store float value 
// float can store integer value
