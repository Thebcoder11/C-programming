#include<stdio.h>
int main(){
    // 1 each statement of c has to written in next line => NO
    // 2 all things written in c are usually in small => YES
    // 3 space is allowed between the c statement => YES
    // 4 space is not allowed in variable name => YES

    return 0;
}
// which of the follwing are wrong statement
// so here wrong statement is 1